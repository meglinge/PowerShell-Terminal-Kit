name: CI

on:
  push:
    branches: ['**']
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5
      - name: Repository verification
        shell: pwsh
        run: ./verify.ps1 -RepositoryOnly
      - name: Installer dry run
        shell: pwsh
        run: ./install.ps1 -SkipPackages -SkipFont -SkipTerminalSettings -DryRun
      - name: Network bootstrap
        shell: pwsh
        env:
          PSTK_BOOTSTRAP_TEST: '1'
        run: Invoke-Expression (Get-Content ./bootstrap.ps1 -Raw)
